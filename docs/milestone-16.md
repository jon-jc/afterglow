# Milestone 16: live proof on Overview

The Overview Run live proof control is now an action button. It reveals and runs the existing proof inline, with progress, receipt inspection and evidence export. The route remains Overview. Campaign assurance retains its selected-campaign inline workflow; the standalone Live proof navigation entry also remains available.

Validation: Go embedded app built; seven real Overview proof checks passed, including route retention, inline evidence, polling preservation and receipt inspection. Seven assurance-inline checks passed. Browser screenshot reviewed and no JavaScript errors reported. Existing assurance regression waits for its page-specific selector now that inline proof can exist on two pages.
